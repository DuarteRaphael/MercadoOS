{
  "name": "@mercados/web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "echo 'No web dev configured yet'",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx",
    "test": "echo 'No web tests configured yet'",
    "format": "prettier --write .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
